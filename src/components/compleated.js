import React from "react";

export default function Compleated(props) {
  return
  <div>
    <h2>{props.newtodo.title}</h2>
    <p>{props.newtodo.description}</p>
    <button> Close </button>
  </div>;
}
